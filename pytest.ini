[pytest]
# Praxis's own suite lives in tests/. Bundled plugins under plugins/ each
# carry their own suite (run from the plugin dir); keep them out of the root run
# to avoid duplicate test-module basename collisions.
testpaths = tests
