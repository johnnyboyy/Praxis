# Config

## project-shape
language: python
framework: none
package-manager: none
has-ui: no
styling: none

# Where this project's domain pool lives. This repo self-hosts: the pool under development IS the
# project's own judgment, so the state dir points at the shipped source pool rather than a separate
# .corpora/domains.
domains-dir: corpora/domains

schema-version: 1
