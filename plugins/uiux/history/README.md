# Historical build-time specs — uiux

Implementation IRs that drove the uiux plugin's build. **Superseded by `uiux_plugin.py`,
`library_state.py`, `deferred_queue.py`, and the `tests/` suite, which are current.**

Kept for design rationale only; do not read as current behavior.

- `UIUX-UPGRADE-SPEC.md` — the IR for turning the bare judgment plugin into a full
  contributor (phases/workflows, library_state, contribute/hooks/surface).
- `DESIGN-SYNC-SPEC.md` — the IR for per-library staleness, the `design-sync` workflow,
  surgical screenshot recapture, and the blast-radius hop.
