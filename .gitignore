__pycache__/
*.pyc
.DS_Store

# praxis runtime artifacts (self-host, debug: yes) — generated, never committed
praxis/handoffs/
praxis/chunks/
praxis/spawn-prompts/
praxis/inbox/
praxis/frames/
praxis/.last-frame-at
# praxis per-root state (frames/markers/payloads are session artifacts, not source)
.praxis/.last-frame-at
.praxis/.frame-payload.md
.praxis/frames/
.praxis/chunks/
.praxis/handoffs/
# conductor per-root runtime state: the event log and the runtime/trace audits are generated by
# work sessions (the fold over them is state, not source) — like the frames/markers above.
.praxis/journal.jsonl
.praxis/runtime-audit.log
.praxis/trace.jsonl
